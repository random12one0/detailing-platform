# His review of every page — 2026-09-10

**62 of 63 looked at, 17 marked to fix.** His own words, pasted from the
checklist at `/map.html`, unedited. Spelling is his; the meaning is not in
doubt anywhere, so nothing is "corrected" here — a tidied quote is a quote
somebody has already interpreted.

**Nothing in this file is done yet.** It is the queue.

---

## Bugs — something did not work

| | Where | His words |
|---|---|---|
| 1 | `/plan/<id>` a plan member's page | *"wasn't able to view it and in Spanish with no way to change it"* |
| 2 | `/job/<id>` one job on its own page | *"nothing showed when clicked on link"* |

Both are "I pressed it and got nothing", which puts them first. The plan page
has a second half — it came up in Spanish **with no way to switch back**, which
is the language picker missing from a page that has one everywhere else.

## Screens that need work

| | Where | His words |
|---|---|---|
| 3 | Reset a password `/reset` | *"no place to put the existing password, also the GUI could use some cleaning"* |
| 4 | How you get paid | *"the something else shouldn't be a switch, it's you actually adding a new one — and when you add it it looks like the others, with a title and then their link or username"* |
| 5 | Notifications | *"the turn on push notifications should be its own big button, more visible"* |
| 6 | Add an expense | *"no way to add a custom name"* |
| 7 | Add a booking by hand | *"make sure they can do everything they would want to, no limitations. Also make it look nicer"* |
| 8 | Common questions | *"should be more obvious it's FAQ"* — note this is the OPPOSITE direction to this morning's rename; he wants the initialism visible, not the plain words alone |
| 9 | Where customers come from | *"should have an arrow to expand a link that shows specific details and stats"* |
| 10 | Monthly plans (settings) | *"the add a monthly plan is a little confusing — more straightforward language and explanation"* |
| 11 | Message templates | *"I feel like there's a better way than having the `{{ }}`"* |
| 12 | First-run setup | *"good except remove the image upload step, and also have some more explaining on what they are filling out"* |
| 13 | Money → the full picture | *"the advanced view needs some work"* — **the only vague one. Ask him what he meant before building.** |
| 14 | Monthly plans (customer-facing, `/book/:slug/plans`) | *"could look better"* |
| 15 | A customer's own booking `/booking/<id>` | *"needs more details"* |

## Language, everywhere

| | His words |
|---|---|
| 16 | Booking rules: *"it's good but some language needs fixing. Not only here but the entire website."* |

**That is a sweep, not a screen.** It is the third time he has asked for a pass
over the words (the label sweep this morning was the second), and this one is
explicitly product-wide.

## He wants to see it, not have it changed

| | His words |
|---|---|
| 17 | Your subscription: *"I want to see how it looks when someone does have a subscription"* |

**Not a defect — a seed.** `node scripts/seed-demo.mjs --subscription=active`
(or whatever that flag really is) and a screenshot answers it.

## Not reached

- **Money → the full picture** was the one row he never opened. Item 13 above
  is about the advanced view, so he clearly did see it; the row is probably
  just unticked.
